# @package eval.tofu.metrics.ra_Q_A_Prob
defaults:
  - ../../data/datasets@datasets: RTL_QA_ra
  - ../../collator@collators: DataCollatorForSupervisedDatasetwithIndex
  # ^ get default dataset and generation config information

handler: probability
batch_size: 32